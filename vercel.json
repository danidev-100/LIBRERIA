{
  "buildCommand": "cd packages/backend && npx prisma generate && pnpm build && cd ../frontend && pnpm build",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
