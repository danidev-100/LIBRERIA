# openspec/config.yaml
# Auto-generated by sdd-init — 2026-06-01
schema: spec-driven

context: |
  Project: LIBRERIA — Bookstore ordering app
  Stack: React (latest), Node.js/Express, Prisma, PostgreSQL, pnpm
  Status: Greenfield — no package.json, no code yet
  Data: ~2000 products in LISTA_ACTUALIZADA_26052026.txt (price catalog)
  Architecture: Client-server SPA (React SPA → Express REST API → PostgreSQL)
  Testing: No test runner installed yet
  Style: Standard JS/TS conventions, to be established

archive:
  - "2026-06-01-scaffold-y-modelo-datos"
  - "2026-06-01-auth-orders-pdf"

rules:
  proposal:
    - Include rollback plan for risky changes
    - State what problem the change solves before describing the solution
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering (1., 1.1, 1.1.1)
    - Keep tasks completable in one session (~30-90 min)
  apply:
    - Follow existing code patterns once established
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
